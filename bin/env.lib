#!/bin/sh

: "${SUDO?}"

source_env_dir() {
  for _source_env_dir in "$1"/*; do
    case "${_source_env_dir##*/}" in
      .DS_Store | .gitkeep | .localized) continue ;;
      sudo.sh) [ "${SUDO-}" ] || continue ;;
    esac
    . "${_source_env_dir}"
  done

  unset _source_env_dir
}

