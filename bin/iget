#!/bin/sh

#
# Silent Curl o wget to stdout

. "$(dirname "$0")/utils.sh"

if has curl; then
  curl -s "$@"
elif has wget; then
  wget -q -O - "$@"
else
    echo "No curl or wget found" >&2
    exit 1
fi
